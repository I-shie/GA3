name: Copy apache repo
on: [push]
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      -  name: Show the current workspace
         run: ls -l
      -  name: Copy Apache Repo
         uses: actions/checkout@v2
         with:
           repository: apache/tomcat
           ref: master
           path: ./tomcat
           
